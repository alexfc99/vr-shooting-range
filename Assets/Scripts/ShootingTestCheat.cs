using UnityEngine;

public class ShootingTestCheat : MonoBehaviour
{

    public WeaponScript weaponScript;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    
}
